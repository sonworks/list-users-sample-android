<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.ListUsersSample" parent="Theme.MaterialComponents.DayNight.NoActionBar.Bridge">
        <item name="colorPrimary">@color/purple_500</item>
    </style>

    <!-- Application theme. -->
    <style name="Theme.ListUsersSample" parent="Base.Theme.ListUsersSample" />

    <style name="Theme.Toolbar" parent="ThemeOverlay.MaterialComponents.Toolbar.Primary">
        <item name="colorOnPrimary">@android:color/white</item>
    </style>
</resources>
